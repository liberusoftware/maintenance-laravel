<div><form wire:submit="{{ $editingPlanId === null ? 'save' : 'update' }}"><input wire:model="name" placeholder="Plan name"><input wire:model="code" placeholder="Code"><input wire:model="frequency_value" type="number" min="1"><button type="submit">{{ $editingPlanId === null ? 'Create plan' : 'Update plan' }}</button>@if ($editingPlanId !== null)<button type="button" wire:click="cancelEdit">Cancel</button>@endif</form>@error('name')<p>{{ $message }}</p>@enderror<ul>@forelse($plans as $plan)<li>{{ $plan->name }} ({{ $plan->frequency_value }} {{ $plan->frequency_unit }}) <button type="button" wire:click="edit({{ $plan->id }})">Edit</button> <button type="button" wire:click="delete({{ $plan->id }})">Delete</button></li>@empty<li>No plans yet.</li>@endforelse</ul></div>
