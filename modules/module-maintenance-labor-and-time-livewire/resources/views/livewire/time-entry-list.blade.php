<div><form wire:submit="{{ $editingEntryId === null ? 'save' : 'update' }}"><textarea wire:model="description" placeholder="Description"></textarea><input wire:model="minutes" type="number" min="1"><button type="submit">{{ $editingEntryId === null ? 'Log time' : 'Update time' }}</button>@if ($editingEntryId !== null)<button type="button" wire:click="cancelEdit">Cancel</button>@endif</form>@error('minutes')<p>{{ $message }}</p>@enderror<ul>@forelse($entries as $entry)<li>{{ $entry->description }} — {{ $entry->minutes }} minutes ({{ $entry->status }}) <button type="button" wire:click="edit({{ $entry->id }})">Edit</button> @if ($entry->status === 'pending')<button type="button" wire:click="reject({{ $entry->id }})">Reject</button>@endif <button type="button" wire:click="delete({{ $entry->id }})">Delete</button></li>@empty<li>No time entries yet.</li>@endforelse</ul></div>
