<div><form wire:submit="save"><textarea wire:model="description" placeholder="Description"></textarea><input wire:model="minutes" type="number" min="1"><button type="submit">Log time</button></form>@error('minutes')<p>{{ $message }}</p>@enderror<ul>@forelse($entries as $entry)<li>{{ $entry->description }} — {{ $entry->minutes }} minutes ({{ $entry->status }})</li>@empty<li>No time entries yet.</li>@endforelse</ul></div>
