<div><form wire:submit="{{ $editingEntryId === null ? 'save' : 'update' }}"><input wire:model="title" placeholder="Schedule title"><input wire:model="starts_at" type="datetime-local"><input wire:model="ends_at" type="datetime-local"><button type="submit">{{ $editingEntryId === null ? 'Schedule' : 'Update schedule' }}</button>@if ($editingEntryId !== null)<button type="button" wire:click="cancelEdit">Cancel</button>@endif</form>@error('starts_at')<p>{{ $message }}</p>@enderror<ul>@forelse($entries as $entry)<li>{{ $entry->title }} — {{ $entry->starts_at }} <button type="button" wire:click="edit({{ $entry->id }})">Edit</button> <button type="button" wire:click="delete({{ $entry->id }})">Delete</button></li>@empty<li>No schedule entries yet.</li>@endforelse</ul></div>
