<div><form wire:submit="save"><input wire:model="title" placeholder="Schedule title"><input wire:model="starts_at" type="datetime-local"><input wire:model="ends_at" type="datetime-local"><button type="submit">Schedule</button></form>@error('starts_at')<p>{{ $message }}</p>@enderror<ul>@forelse($entries as $entry)<li>{{ $entry->title }} — {{ $entry->starts_at }}</li>@empty<li>No schedule entries yet.</li>@endforelse</ul></div>
