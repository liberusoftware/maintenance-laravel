<div><form wire:submit="{{ $editingRequestId === null ? 'save' : 'update' }}"><input wire:model="title" placeholder="Request title"><input wire:model="amount" type="number" min="0" step="0.01"><button type="submit">{{ $editingRequestId === null ? 'Request purchase' : 'Update request' }}</button>@if ($editingRequestId !== null)<button type="button" wire:click="cancelEdit">Cancel</button>@endif</form>@error('title')<p>{{ $message }}</p>@enderror<ul>@forelse($requests as $request)<li>{{ $request->title }} — {{ $request->amount }} {{ $request->currency }} ({{ $request->status }}) <button type="button" wire:click="edit({{ $request->id }})">Edit</button> @if ($request->status === 'pending')<button type="button" wire:click="reject({{ $request->id }})">Reject</button>@endif <button type="button" wire:click="delete({{ $request->id }})">Delete</button></li>@empty<li>No purchase requests yet.</li>@endforelse</ul></div>
