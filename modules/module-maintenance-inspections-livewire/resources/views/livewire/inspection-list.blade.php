<div><form wire:submit="{{ $editingInspectionId === null ? 'save' : 'update' }}"><input wire:model="title" placeholder="Inspection title"><input wire:model="template_key" placeholder="Template"><button type="submit">{{ $editingInspectionId === null ? 'Create inspection' : 'Update inspection' }}</button>@if ($editingInspectionId !== null)<button type="button" wire:click="cancelEdit">Cancel</button>@endif</form>@error('title')<p>{{ $message }}</p>@enderror<ul>@forelse($inspections as $inspection)<li>{{ $inspection->title }} ({{ $inspection->outcome }}) <button type="button" wire:click="edit({{ $inspection->id }})">Edit</button> <button type="button" wire:click="delete({{ $inspection->id }})">Delete</button></li>@empty<li>No inspections yet.</li>@endforelse</ul></div>
